1,076 characters — well within budget, and all sections are correct. Pass: 10 matches what we saw earlier.

`.lathe/snapshot.sh` is written and executable. It covers:
- **Git status + recent commits** — cycle-to-cycle diff signal
- **Build** (`go build`) — silent on success, shows errors on failure
- **Vet** (`go vet`) — same pattern
- **Tests** (`go test -v -count=1`) — summarized as `Pass: 10 | Fail: 0 | Packages OK: 1 | Packages FAIL: 0`, expands only on failure
- **CI** — lists workflow filenames
- **Module** — go.mod header for version context

Output is ~1,100 chars, leaving ~4,900 chars of headroom for failure detail expansion.
